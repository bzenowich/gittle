# Build settings for every `nim c` in this project.
--path:"src"
--styleCheck:hint
--warning:"ProveInit:off"

# Decision 4: the system zlib is the one external dependency.
--passL:"-lz"

# One static binary (plan.md 1).  `nim c -d:static ...`
@if static:
  --passL:"-static"
@end
